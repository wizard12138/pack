# 银河麒麟 ARM64 版本 · 构建说明文档

本文件夹为「银河麒麟 ARM64 版本」的**独立交付包**，可直接推送为一个 GitHub 仓库，
由 GitHub Actions 构建出可在麒麟 ARM64 上离线运行的程序。

**启动方式**：使用 `start.sh` 脚本启动（需系统已安装 Python 3.8+）。

---

## 一、核心原理：为什么要放进容器里构建

这是本次构建最关键的坑，务必先理解：

| 系统 | 架构 | glibc |
|---|---|---|
| **银河麒麟 Server/Desktop V10（CentOS 系）** | ARM64 | **约 2.28** |
| Ubuntu 20.04 | ARM64 | 2.31 |
| Ubuntu 22.04 | ARM64 | 2.35 |

**glibc 只保证向后兼容**：

- 在 glibc **2.28** 上构建的二进制 → 可运行于 2.28 / 2.31 / 2.35 系统 ✅
- 在 glibc **2.35** 上构建的二进制 → **无法**运行于 2.28 的麒麟，
  报错 `version 'GLIBC_2.35' not found` ❌

因此本方案在 **Rocky Linux 8（aarch64，glibc 2.28）容器**中构建。
它与麒麟同源、glibc 足够旧，产物可同时覆盖：
银河麒麟 Server/Desktop V10、Ubuntu 20.04+ ARM64。

> 若实测目标麒麟的 glibc 低于 2.28，需把构建基线下调到对应发行版。

---

## 二、文件清单

```
3-麒麟ARM64版本/
├── backend/                                 源码
│   ├── server.py                           Flask 入口（含冻结路径解析）
│   ├── excel_handler.py                    Excel 生成核心
│   ├── form_processor.py                   表单校验
│   ├── requirements.txt                    flask==3.0.0 / openpyxl==3.1.5
│   ├── static/                             前端页面（index.html、贴票助手、pdf.js、样式）
│   └── templates/差旅费报销相关表格.xlsx       4 张报销表格模板
├── scripts/build_in_container.sh           容器内构建脚本（start.sh 方式）
├── Dockerfile.kylin-arm64                  麒麟构建环境镜像（本地复现用）
├── .github/workflows/
│   ├── build-kylin-arm64.yml               主工作流（原生 ARM64 runner）★首选
│   └── build-kylin-arm64-qemu.yml          备用工作流（QEMU 模拟，慢）
├── .gitignore                              忽略构建产物
├── 提示词.md                                交给执行者的任务提示词
└── 说明文档.md                               本文件
```

---

## 三、工作流说明

### 主工作流 `build-kylin-arm64.yml`（首选，约 3–5 分钟）
- `runs-on: ubuntu-22.04-arm`（GitHub 托管 ARM64 runner，原生构建）
- 在 `rockylinux:8` aarch64 容器中执行 `scripts/build_in_container.sh`
- 产出 `finance-kylin-arm64.tar.gz` 并上传为 Artifact

### 备用工作流 `build-kylin-arm64-qemu.yml`（约 20–40 分钟）
适用于**私有仓库**没有 ARM64 runner 额度的情况：
在 x86 runner 上通过 QEMU 模拟 aarch64，速度慢但结果一致。

### 自托管 runner（可选，最贴合目标环境）
若有一台银河麒麟 ARM64 机器，可直接注册为 self-hosted runner，
把主工作流中 `runs-on: ubuntu-22.04-arm` 改为 `runs-on: self-hosted`，
在麒麟上原生构建（此时可跳过容器，直接执行 `scripts/build_ci.sh` 思路的本机构建）。

**Runner 可用性**：`ubuntu-22.04-arm` 对**公开仓库**免费可用；
私有仓库通常需 Team/Enterprise 计划。

---

## 四、如何触发

1. **手动**：仓库 → `Actions` → `Build Kylin ARM64` → `Run workflow`
2. **打 tag**（额外发布到 Release）：
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```

构建完成后在 run 页面底部 **Artifacts** 下载 `finance-kylin-arm64`。

---

## 五、目标机部署与验收

```bash
# 1. 解压
tar xzf finance-kylin-arm64.tar.gz
cd 财务报销系统

# 2. 赋予执行权限
chmod +x start.sh

# 3. 确认 glibc 满足要求（麒麟应 >= 2.28）
ldd --version | head -1

# 4. 确认 Python 已安装（需 3.8+）
python3 --version

# 5. 启动服务
./start.sh
```

**验收清单**
- [ ] 控制台输出「启动财务报销系统」，访问地址 `http://localhost:5000`
- [ ] 浏览器打开首页正常
- [ ] `curl http://127.0.0.1:5000/api/health` → `{"status":"ok","template_exists":true}`
- [ ] 勾选「差旅费报销凭证」成功导出 xlsx（不报 500）
- [ ] 四张表全选导出生成 zip，内容正确
- [ ] **断网**后重复上述步骤，确认完全离线可用

---

## 六、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 麒麟启动报 `GLIBC_2.35 not found` | 在高 glibc 环境构建 | 改用 Rocky 8 容器构建（本方案） |
| ARM64 runner 报错「not supported」 | 私有仓库无 ARM64 额度 | 用 QEMU 备用工作流或自托管 runner |
| 中文文件名乱码 | 容器 locale 非 UTF-8 | 已设 `PYTHONUTF8=1`；检查目标机 locale |
| 报错 "python: command not found" | 系统未安装 Python 3.8+ | `sudo dnf install python38` 或 `sudo apt install python3.8` |
| 报错 "No module named flask" | 未使用内置虚拟环境 | 确保使用 `./start.sh` 启动，或手动激活 `.venv` |
| 导出报销凭证报 500 | openpyxl 版本被改回 3.1.2 | 锁定 `openpyxl==3.1.5`，勿降级 |
