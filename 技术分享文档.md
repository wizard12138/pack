# 银河麒麟 ARM64 版本构建技术分享

> 本文档整理了「财务报销系统」银河麒麟 ARM64 版本的完整构建流程，适合没有接触过跨架构构建的同学学习。

---

## 一、项目背景

### 1.1 业务需求
财务报销系统是一个基于 Flask + openpyxl 的 Web 应用，需要在银河麒麟 ARM64 服务器上离线运行。

### 1.2 技术挑战
**核心问题：glibc 兼容性**

| 系统 | 架构 | glibc 版本 |
|---|---|---|
| 银河麒麟 V10 | ARM64 | 2.28 |
| Ubuntu 20.04 | ARM64 | 2.31 |
| Ubuntu 22.04 | ARM64 | 2.35 |

**glibc 只保证向后兼容**：
- ✅ 在 glibc 2.28 上构建的二进制 → 可运行于 2.28 / 2.31 / 2.35
- ❌ 在 glibc 2.35 上构建的二进制 → 无法运行于 2.28 的麒麟

**解决方案**：在 Rocky Linux 8（aarch64，glibc 2.28）容器中构建，确保产物兼容性。

---

## 二、技术架构

### 2.1 整体流程
```
源码 → GitHub 仓库 → GitHub Actions → 容器构建 → 产物上传 → 下载部署
```

### 2.2 关键组件
- **PyInstaller**：Python 程序打包工具，生成独立可执行文件
- **Docker 容器**：Rocky Linux 8 aarch64，提供低 glibc 基线环境
- **GitHub Actions**：CI/CD 平台，自动化构建流程
- **ARM64 Runner**：GitHub 托管的 ARM64 构建环境

### 2.3 文件结构
```
3-麒麟ARM64版本/
├── backend/                          # 源码
│   ├── server.py                     # Flask 入口
│   ├── excel_handler.py              # Excel 生成核心
│   ├── form_processor.py             # 表单校验
│   ├── requirements.txt              # 依赖清单
│   ├── static/                       # 前端页面
│   └── templates/                    # Excel 模板
├── build.spec                        # PyInstaller 配置
├── scripts/build_in_container.sh     # 容器内构建脚本
├── Dockerfile.kylin-arm64            # 构建环境镜像
├── .github/workflows/
│   ├── build-kylin-arm64.yml         # 主工作流（ARM64 Runner）
│   └── build-kylin-arm64-qemu.yml    # 备用工作流（QEMU 模拟）
├── .gitignore                        # 忽略构建产物
├── 提示词.md                          # 任务提示词
└── 说明文档.md                        # 本文档
```

---

## 三、构建流程详解

### 3.1 环境准备

#### 3.1.1 安装 Git
```bash
# Windows（通过 winget）
winget install Git.Git

# 验证安装
git --version
```

#### 3.1.2 配置 Git 用户信息
```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

#### 3.1.3 准备 GitHub Personal Access Token (PAT)
1. 访问 GitHub Settings → Developer settings → Personal access tokens
2. 生成新 token，勾选 `repo` 权限
3. 保存 token 到安全位置

### 3.2 仓库初始化与推送

#### 3.2.1 初始化本地仓库
```bash
cd 3-麒麟ARM64版本
git init
git add .
git commit -m "feat: 银河麒麟 ARM64 版本 - 初始提交"
```

#### 3.2.2 推送到 GitHub
```bash
# 添加远程仓库（使用 PAT 认证）
git remote add origin https://YOUR_PAT@github.com/username/repo.git

# 推送代码
git push -u origin main

# 创建标签（触发自动构建）
git tag v1.0.0
git push origin v1.0.0
```

### 3.3 GitHub Actions 工作流

#### 3.3.1 主工作流（build-kylin-arm64.yml）
```yaml
name: Build Kylin ARM64

on:
  workflow_dispatch:        # 手动触发
  push:
    tags:
      - 'v*'               # 标签推送触发

permissions:
  contents: write           # Release 写入权限

jobs:
  build-kylin-arm64:
    name: Kylin ARM64 (glibc 2.28)
    runs-on: ubuntu-22.04-arm    # ARM64 Runner
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 确认架构为 aarch64
        run: uname -m

      - name: 在 Rocky Linux 8 容器中构建
        env:
          PYTHONUTF8: '1'
        run: |
          docker run --rm \
            -v "$GITHUB_WORKSPACE":/src \
            -w /src \
            -e PYTHONUTF8=1 \
            rockylinux:8 \
            bash scripts/build_in_container.sh

      - name: 打包
        run: |
          cd dist
          tar czf ../finance-kylin-arm64.tar.gz 财务报销系统

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: finance-kylin-arm64
          path: finance-kylin-arm64.tar.gz

      - name: 发布到 Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: finance-kylin-arm64.tar.gz
```

#### 3.3.2 容器内构建脚本（build_in_container.sh）
```bash
#!/bin/bash
set -e
cd "$(dirname "$0")/.."

echo "===== 容器环境信息 ====="
uname -m
. /etc/os-release && echo "$PRETTY_NAME"
ldd --version 2>/dev/null | head -1

echo "===== 安装 Python 3.9 与编译工具 ====="
dnf install -y python39 python39-devel gcc make tar gzip
dnf install -y python39-pip || true

PY=python3.9
"$PY" --version

# 若 pip 未随包提供，用 ensurepip 引导
"$PY" -m pip --version >/dev/null 2>&1 || "$PY" -m ensurepip --upgrade

echo "===== 准备构建虚拟环境 ====="
"$PY" -m venv /build-venv
. /build-venv/bin/activate

pip install --no-cache-dir --upgrade pip
pip install --no-cache-dir pyinstaller
pip install --no-cache-dir -r backend/requirements.txt

echo "===== 清理旧产物 ====="
rm -rf build dist

echo "===== PyInstaller 构建（onedir）====="
PYTHONUTF8=1 pyinstaller build.spec --noconfirm --clean

echo "===== 产物清单 ====="
ls -la dist/财务报销系统
echo ""
echo "可执行文件: dist/财务报销系统/财务报销系统"
echo "glibc 基线约 2.28，可运行于银河麒麟 ARM64 及 Ubuntu 20.04+ ARM64。"
echo "构建完成。"
```

### 3.4 PyInstaller 配置（build.spec）
```python
# -*- mode: python ; coding: utf-8 -*-
import os

ROOT = os.path.abspath(SPECPATH)

a = Analysis(
    [os.path.join(ROOT, 'backend', 'server.py')],
    pathex=[os.path.join(ROOT, 'backend')],
    binaries=[],
    datas=[
        (os.path.join(ROOT, 'backend', 'templates'), 'templates'),
        (os.path.join(ROOT, 'backend', 'static'), 'static'),
    ],
    hiddenimports=['openpyxl', 'flask', 'werkzeug', 'jinja2', 'form_processor'],
    excludes=[],
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='财务报销系统',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='财务报销系统',
)
```

---

## 四、常见问题与解决方案

### 4.1 glibc 版本不兼容
**现象**：麒麟启动报 `GLIBC_2.35 not found`
**原因**：在高 glibc 环境构建
**解决**：使用 Rocky Linux 8 容器（glibc 2.28）构建

### 4.2 ARM64 Runner 不可用
**现象**：私有仓库报错「not supported」
**原因**：GitHub ARM64 Runner 对私有仓库有限制
**解决**：
- 使用 QEMU 备用工作流（较慢）
- 或注册自托管 Runner

### 4.3 中文文件名乱码
**现象**：容器 locale 非 UTF-8
**解决**：设置 `PYTHONUTF8=1` 环境变量

### 4.4 PyInstaller 报缺编译工具
**现象**：报错缺 gcc / Python.h
**解决**：容器中安装 `gcc`、`python39-devel`

### 4.5 导出报销凭证报 500
**现象**：openpyxl 版本被改回 3.1.2
**解决**：锁定 `openpyxl==3.1.5`，勿降级

---

## 五、验收清单

在银河麒麟 ARM64 目标机上执行：

```bash
# 1. 解压
tar xzf finance-kylin-arm64.tar.gz
cd 财务报销系统

# 2. 赋予执行权限
chmod +x 财务报销系统

# 3. 确认 glibc 满足要求
ldd --version | head -1

# 4. 启动
./财务报销系统

# 5. 验证服务
curl http://127.0.0.1:5000/api/health
# 应返回: {"status":"ok","template_exists":true}

# 6. 测试导出功能
# 勾选「差旅费报销凭证」，确认导出 xlsx 不报 500

# 7. 断网测试
# 断开网络，重复上述步骤，确认完全离线可用
```

---

## 六、学习要点总结

### 6.1 跨架构构建的核心思想
- **不能跨架构编译**：ARM64 必须在 ARM64 环境上构建
- **glibc 向后兼容**：低版本构建，高版本运行
- **容器化环境**：确保构建环境一致性

### 6.2 CI/CD 最佳实践
- **自动化构建**：GitHub Actions 实现持续集成
- **多平台支持**：主工作流 + 备用工作流
- **产物管理**：Artifact 上传 + Release 发布

### 6.3 Python 打包注意事项
- **PyInstaller onedir 模式**：适合静态资源较大的应用
- **中文文件名支持**：设置 PYTHONUTF8=1
- **依赖锁定**：requirements.txt 精确版本控制

### 6.4 安全与权限
- **PAT 权限最小化**：仅勾选必要权限
- **.gitignore 配置**：排除构建产物和敏感信息
- **Release 权限**：workflow 中声明 contents: write

---

## 七、扩展学习资源

### 7.1 官方文档
- [GitHub Actions 文档](https://docs.github.com/cn/actions)
- [PyInstaller 文档](https://pyinstaller.org/)
- [Docker 文档](https://docs.docker.com/)

### 7.2 相关技术
- **QEMU**：跨架构模拟器，用于在 x86 上模拟 ARM64
- **Docker Buildx**：多平台镜像构建
- **Self-hosted Runner**：自托管构建环境

### 7.3 进阶话题
- 多架构 Docker 镜像构建
- 交叉编译工具链
- 系统级依赖管理

---

## 八、附录

### 8.1 完整命令速查表

```bash
# 环境准备
winget install Git.Git
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 仓库操作
git init
git add .
git commit -m "feat: 初始提交"
git remote add origin https://YOUR_PAT@github.com/username/repo.git
git push -u origin main
git tag v1.0.0
git push origin v1.0.0

# 构建验证
git log --oneline
git remote -v
git status
```

### 8.2 关键文件说明

| 文件 | 作用 |
|---|---|
| `build.spec` | PyInstaller 配置，定义打包规则 |
| `build_in_container.sh` | 容器内构建脚本，安装依赖并执行打包 |
| `build-kylin-arm64.yml` | GitHub Actions 主工作流 |
| `Dockerfile.kylin-arm64` | 构建环境镜像定义 |
| `.gitignore` | Git 忽略规则，排除构建产物 |

### 8.3 版本信息

- **Python**: 3.9
- **Flask**: 3.0.0
- **openpyxl**: 3.1.5
- **PyInstaller**: 最新版
- **glibc 基线**: 2.28（Rocky Linux 8）
- **目标平台**: 银河麒麟 ARM64（glibc 2.28+）

---

**分享人**：Reasonix  
**分享时间**：2026-09-03  
**项目地址**：https://github.com/wizard12138/pack  
**构建产物**：finance-kylin-arm64.tar.gz
