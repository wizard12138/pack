# 任务提示词：构建银河麒麟 ARM64 版本（交给 Reasonix）

> 使用方式：把下面「可直接复制的任务提示词」整段复制给 Reasonix，并把本文件夹
> （`3-麒麟ARM64版本`）作为工作目录 / 推送为独立仓库。

---

## 可直接复制的任务提示词

```
请为「财务报销系统」构建银河麒麟 ARM64 版本的可执行程序，通过 GitHub Actions 完成。

【项目背景】
财务报销系统：Flask 后端 + openpyxl 生成 Excel，前端为纯静态页面。
运行时完全离线、不联网。项目已完成 PyInstaller 化改造，build.spec 已就绪。
Windows 版本已构建完成，你只需处理银河麒麟 ARM64 这一个平台。

【最重要的技术约束 —— 请务必理解并严格遵守】
1. PyInstaller 不能跨架构/跨系统编译，ARM64 必须在 ARM64 环境上构建。
2. glibc 只保证「向后兼容」，不保证「向前兼容」：
   - 银河麒麟 V10（CentOS 系）glibc 约 2.28
   - Ubuntu 22.04 ARM64 的 glibc 是 2.35
   - 在 glibc 2.35 环境构建的二进制，拷到麒麟上会直接报
     「version `GLIBC_2.35' not found」而无法运行
   因此：必须在「低 glibc 基线」环境中构建。
   本文件夹已备好方案 —— 在 Rocky Linux 8（aarch64，glibc 2.28）容器内构建，
   产物可向前兼容银河麒麟与 Ubuntu 20.04+ ARM64。
   请【不要】改成直接在 ubuntu-22.04-arm 裸机 runner 上构建。

【本文件夹内已为你准备的文件】
- backend/                                 源码（server.py / excel_handler.py 等）
- backend/static/                          前端页面
- backend/templates/差旅费报销相关表格.xlsx   4 张报销表格模板
- backend/requirements.txt                 运行时依赖 flask==3.0.0 / openpyxl==3.1.5
- build.spec                               PyInstaller 配置（已调试通过，勿改）
- scripts/build_in_container.sh            容器内构建脚本（麒麟专用）
- Dockerfile.kylin-arm64                   麒麟构建环境镜像（本地复现用）
- .github/workflows/build-kylin-arm64.yml      主工作流（原生 ARM64 runner）
- .github/workflows/build-kylin-arm64-qemu.yml 备用工作流（QEMU 模拟，较慢）
- .gitignore                               已忽略 dist/ build/ 等产物
- 说明文档.md                               完整说明（原理、触发方式、验收清单）

【你要做的事】
1. 阅读「说明文档.md」，理解 glibc 兼容性原理与工作流设计。
2. 检查 build-kylin-arm64.yml 与 scripts/build_in_container.sh 是否正确，
   有无笔误或新版 Actions 兼容性问题，必要时修正。
3. 确认 ARM64 runner 方案：
   - 公开仓库：可直接使用 ubuntu-22.04-arm，主工作流开箱即用；
   - 私有仓库且无 ARM64 runner 额度：改用 QEMU 备用工作流，
     或把 runs-on 改为 self-hosted 并在文档中说明如何注册自托管 runner。
4. 确保工作流可通过「Actions → Run workflow」手动触发，
   且打 tag（v*）时自动触发并发布到 Release。
5. 提交推送，触发真实构建，直到 job 成功（绿色）。

【验收标准】
- job 构建成功，产出可下载的 Artifact：finance-kylin-arm64.tar.gz
- 压缩包解压后含完整可运行目录 财务报销系统/
  （内含可执行文件 与 _internal 依赖目录）
- 在银河麒麟 ARM64 目标机上能启动服务
- /api/health 返回 {"status":"ok","template_exists":true}
- 勾选「差旅费报销凭证」能成功导出 xlsx（不报 500）
- 断网状态下仍可正常运行（确认完全离线）

【注意事项 / 红线】
- 不要提交构建产物：dist/、build/、.build-venv/ 已在 .gitignore 中
- 产物目录名与可执行文件名含中文「财务报销系统」，需保证 UTF-8 环境；
  工作流与脚本中已设 PYTHONUTF8=1，【不要删除】
- 【不要】把 openpyxl 改回 3.1.2：其富文本在 3.1.2 下有已知缺陷，
  会导致「差旅费报销凭证」导出报 500（已修复并锁定 3.1.5）
- 【不要】改成 onefile 单文件模式：当前为 onedir 单文件夹模式
  （静态资源含 3MB 贴票助手与 1MB pdf.worker，单文件每次启动都要解包，很慢）
- 【不要】改动 backend/ 下的业务代码，本次只做构建与打包
```

---

## 派活前请你先确认（避免返工）

1. **目标麒麟的具体版本与 glibc**
   在麒麟机器上执行 `ldd --version | head -1` 确认。
   若低于 2.28，需把构建基线下调（换更旧的基础镜像），请提前告知执行者。

2. **仓库可见性**
   - 公开仓库 → `ubuntu-22.04-arm` 直接免费可用，主工作流开箱即用。
   - 私有仓库 → 确认是否有 ARM64 runner 额度；没有就走 QEMU 备用工作流或自托管 runner。

3. **必须在真机实测**
   CI 只能保证「构建成功」，不能保证麒麟上真能跑。
   拿到 `finance-kylin-arm64.tar.gz` 后，按「说明文档.md」第五节在真实麒麟机器上验收。

4. **Release 权限**
   打 tag 自动发布需要 `contents: write` 权限（工作流已声明）。
   若组织默认权限为只读，需在仓库 Settings → Actions 中放开。
